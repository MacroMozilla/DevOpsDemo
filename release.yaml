project:
  name: django-app
  description: |
    Django CI & Docker Build pipeline
    - Automated lint and unit testing
    - Build and push Docker images to Docker Hub
    - Send deployment notifications to Slack

version: "0.0.1"
changes:
  added:
    - None
  fixed:
    - None
  changed:
    - change version standard